import foo;
import foo.bar;
import foo.bar.bas;

import foo as zim;
import foo.bar as zang;
import foo.bar.bas as zung;

import foo.*;
import foo.bar.*;
import foo.bar.bas.*;

import foo.bar.bas.();

import foo.bar.bas.(a);

import foo.bar.bas.(a, b);
import foo.bar.bas.(a, b,);

import foo.bar.bas.(a, b as c);
import foo.bar.bas.(a as b, c);
import foo.bar.bas.(
    a as b,
    c as d,
);

public import foo.*;
private import foo.*;

import foo.(private a);
import foo.(private a as b);
