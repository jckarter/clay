println() { }

foo(x) { println(x); }
private bar(x) { println(x); }
public bas(x) { println(x); }

static Foo('x) = 'x;
static Bar('x) | Foo('x) = ('x, 'x);

inline foo(x) { println(x); }
private inline foo(x) { println(x); }
inline overload foo(x) { println(x); }
