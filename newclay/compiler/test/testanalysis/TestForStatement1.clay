// don't do this at home
alias IntIterator = Tuple[Int, Int];

overload iterator(x:Int) = (0, x);
overload hasFront?(i:IntIterator) = i.0 < i.1;
overload front(i:IntIterator) = i.0;
overload incFront(ref i:IntIterator) { i.0 += 1; }

foo(x:Int) {}

main() {
    for (x in 5)
        foo(x);
}
