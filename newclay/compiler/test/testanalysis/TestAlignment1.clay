symbol IntA16;

overload #type?(IntA16) = true;
overload #llType(IntA16) = llIntegerType[llSigned, 32];
overload #typeAlign(IntA16) = 16u;

foo() --> returned:IntA16 __c__ """abort();""";
bar(x) __c__ """abort();""";

main() {
    var x = foo();
    bar(x);
    var y = 16;
    bar(y);
}
