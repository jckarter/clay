import vectors.*;
import show.*;

main() {
    var v = Vector[Int](2, 3, 5, 7, 11);

    show("---");
    for (x in v)
        show(x);

    for (ref x in v)
        x *= 2;

    show("---");
    for (x in v)
        show(x);
}
