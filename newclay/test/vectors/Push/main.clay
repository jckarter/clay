import containers.(push);
import vectors.(Vector);
import show.(show);

main() {
    var v = Vector[Int]();
    
    push(v, 2);
    push(v, 3, 5);
    push(v, Array[Int,2](7, 11));

    for (x in v)
        show(x);
}
