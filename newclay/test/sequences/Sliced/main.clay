import sequences.(Sliced);
import show.(show);
import vectors.(Vector);

main() {
    var v = Vector[Int](0, 0, 0, 1, 1, 2);

    for (x in Sliced(v, 3, 5))
        show(x);

    for (x in Sliced(Vector[Int](1, 1, 1, 2, 2, 3), 3, 5))
        show(x);
}
