import show.(show);
import __primitives__.*;
import factorial.*; // has Factorial
import privatetools.*; // also has Factorial, but private

define Main;
overload #Main() = Factorial(8);

main() { show(..#Main()); }
