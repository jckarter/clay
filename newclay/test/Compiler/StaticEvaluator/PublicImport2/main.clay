import show.(show);
import __primitives__.*;
import __primitives__.(staticModuleMember as m);
import publicimporter;

define Main;
overload #Main() = [
    m(publicimporter, "Factorial")(8),
    m(publicimporter, "Fac")(8),
    m(m(publicimporter, "factorial"), "Factorial")(8),
    m(m(publicimporter, "fac"), "Factorial")(8),
];

main() { show(..#Main()); }
