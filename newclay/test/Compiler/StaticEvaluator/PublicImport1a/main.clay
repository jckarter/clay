import show.(show);
import __primitives__.*;
import publicimporter.*;

define Main;
overload #Main() = [
    Factorial(8),
    Fac(8),
    factorial.Factorial(8),
    fac.Factorial(8),
];

main() { show(..#Main()); }
