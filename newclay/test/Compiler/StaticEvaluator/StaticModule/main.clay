import show.(show);
import __primitives__.*;
import __primitives__;
import factorial.*;
import factorial;

define Foo['X];
define Bar;

#Main()
    = staticModule(staticModule),
      staticModule(Factorial),
      staticModule(Foo),
      staticModule(Foo[Bar]);

main() { show(..#Main()); }
