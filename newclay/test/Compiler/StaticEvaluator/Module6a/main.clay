import show.(show);
import __primitives__.*;
import tools;
import tools.factorial;

define Main;
overload #Main() = [
    tools.Factorial(8),
    tools.factorial.Factorial(8),
];

main() { show(..#Main()); }
