import show.(show);
import values.(dropValues);
import __primitives__.*;

symbol wut = "éééé";

#foo() = [x -> {
    return x + 1;
}];

#Main() = ..dropValues(1, ..lambdaLocation(foo()));

main() { show(..#Main()); }
