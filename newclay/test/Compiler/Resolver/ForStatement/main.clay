import show.(show);
import __primitives__.*;

define Foo as ();
define bar as ();
define barrr as ();
define bas as ();

overload Foo() {
    for (bar, barr in bar)
        var ..barrr = bar, barr, bas;
    bar;
    bas;
    barrr;
}

main() { show("ok"); }
