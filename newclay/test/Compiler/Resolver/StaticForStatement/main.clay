import show.(show);
import __primitives__.*;

define Foo as ();
define bar as ();
define barrr as ();
define bas as ();

overload Foo() {
    static for (bar in bar, bas)
        var ..barrr = bar, bas;
    bar;
    bas;
    barrr;
}

main() { show("ok"); }
