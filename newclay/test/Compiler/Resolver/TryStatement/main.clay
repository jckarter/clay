import show.(show);
import __primitives__.*;

define Foo as ();
define bar as ();
define bas as ();

overload Foo() {
    try {
        bar;
        bas;
    } catch (bar:Foo) {
        bar;
        bas;
    } catch (bas) {
        bar;
        bas;
    }
    bar;
    bas;
}

main() { show("ok"); }
