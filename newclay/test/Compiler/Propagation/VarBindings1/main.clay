import show.(show);

foo() = 1, true, "three";
bar() = 4.0;

main() {
    var x0 = bar();
    var x1, y1, z1 = foo();
    var ..w1 = foo();
    var x2, y2, z2 = ..foo();
    var x3, y3, ..z3 = ..foo(), bar();

    show(..#type(->  (x0, x1, y1, z1, ..w1, x2, y2, z2, x3, y3, ..z3) ));
}
