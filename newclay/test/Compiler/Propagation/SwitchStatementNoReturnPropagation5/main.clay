import show.(show);

foo() {
    switch (0)
    case (1) {
        throw 0;
    }
    case (2) {
        throw 0;
    }
    else {
        throw 0;
    }
}

main() {
    show(#returns?(-> { foo(); }));
}
