import show.(show);

foo() {
    switch (0)
    case (1) {
        throw 0;
    }
    case (2) {
        return 2;
    }
    else {
        return 3;
    }
}

main() {
    show(#returns?(-> { foo(); }));
}
