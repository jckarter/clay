import show.*;
import unsafe.valuesemantics.(PODType?);

main() {
    show(#PODType?(Tuple[])); ////true
    show(#TypeSize(Tuple[])); ////0u
    show(#TypeAlign(Tuple[])); ////1u

    var t = Tuple[]();

    show(..*t);
}
