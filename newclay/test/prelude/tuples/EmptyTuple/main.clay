import show.*;
import unsafe.valuesemantics.(POD?);

main() {
    show(#POD?(Tuple[])); ////true
    show(#typeSize(Tuple[])); ////0u
    show(#typeAlign(Tuple[])); ////1u

    var t = Tuple[]();

    show(..*t);
}
