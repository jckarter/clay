import show.*;
import vectors.*;

foo(x) = (y) -> (z) -> x + y + z;

main() {
    var f = foo(2);
    var g = foo(3);

    var ff = f(5);
    var fg = f(7);
    var gf = g(11);
    var gg = g(13);

    show(ff(1)); ////8
    show(fg(1)); ////10
    show(gf(1)); ////15
    show(gg(1)); ////17
}
