import containers.*;
import show.*;
import vectors.*;

main() {
    var v = Vector[Int](2, 3, 5);

    var f = (x) => pop(v) + x;

    show(f(1)); ////6
    show(f(1)); ////4
    show(size(v)); ////3
    show(back(v)); ////5
}
