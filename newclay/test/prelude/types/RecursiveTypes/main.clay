import show.*;

symbol Foo = RecordType(p:Pointer[Foo]);

symbol Bar1 = RecordType(p:Pointer[Bar2]);
symbol Bar2 = RecordType(p:Pointer[Bar1]);

main() {
    show(#type?(Foo)); ////true
    show(#type?(Bar1)); ////true
    show(#type?(Bar2)); ////true
}
