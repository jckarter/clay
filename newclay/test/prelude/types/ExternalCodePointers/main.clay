import show.*;

main() {
    show("--no output type"); ////--no output type
    show(..#externalOutputTypes(()));
    show("--with output type"); ////--with output type
    show(..#externalOutputTypes(Int)); ////Int
}
