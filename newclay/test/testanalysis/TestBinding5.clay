symbol foo = NewProcedure();
overload foo(x:'T) {
    var y:'T = x;
    return y;
}

symbol main = NewProcedure();
overload main() {
    var x:Int = 5;
    var y:Bool = foo(true);
    return x, y;
}

