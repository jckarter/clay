import containers.(Pusher);
import show.(show);
import vectors.(Vector);

main() {
    var xs = Vector[Int]();

    var p = Pusher(xs);
    write1(p, 1);
    write1(p, 11);
    write1(p, 21);
    write1(p, 1211);

    for (x in xs)
        show(x);
}
