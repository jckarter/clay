import containers.(Inserter);
import show.(show);
import vectors.(Vector);

main() {
    var xs = Vector[Int](-1, -1);

    var i = Inserter(xs, 1);
    write1(i, 1);
    write1(i, 11);
    write1(i, 21);
    write1(i, 1211);

    for (x in xs)
        show(x);
}
