import printer.*;

define foo private overload;

override foo() = 0;
override foo(x) = 1;

define bar(a, b) private overload;

override bar(a, b) = a + b;

main() {
    println(foo());
    println(foo(true));
    println(bar(1, 1));
}
