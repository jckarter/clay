import printer.*;

define foo private overload;

overload foo() = 0;
overload foo(x) = 1;

define bar(a, b) private overload;

overload bar(a, b) = a + b;

main() {
    println(foo());
    println(foo(true));
    println(bar(1, 1));
}
