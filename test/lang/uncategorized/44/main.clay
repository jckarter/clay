import printer.(println);

define wrap;
wrap(rvalue first, forward ..rest) = first, ..wrap(..rest);
wrap(ref first, forward ..rest) = @first, ..wrap(..rest);
wrap() = ;

main() {
    var x = 10;
    var a = [..wrap(10, x, 10, x)];
    println(Type(a));
}
