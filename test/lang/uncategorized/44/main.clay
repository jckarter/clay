import printer.(println);

define wrap;
override wrap(rvalue first, forward ..rest) = first, ..wrap(..rest);
override wrap(ref first, forward ..rest) = @first, ..wrap(..rest);
override wrap() = ;

main() {
    var x = 10;
    var a = [..wrap(10, x, 10, x)];
    println(Type(a));
}
