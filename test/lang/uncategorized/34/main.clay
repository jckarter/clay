import printer.(println);
record Foo ();

override Foo() --> returned:Foo { println("constructing Foo"); }
override destroy(x:Foo) { println("destroying Foo"); }

var foo = Foo();

main() {
    foo;
}
