import printer.(println);

define printValue;

printValue(x:Int32) {
    println("int value = ", x);
}

printValue(x:UInt32) {
    println("uint value = ", x);
}

printValue(x:Float64) {
    println("double value = ", x);
}

main() {
    var a = 1 + 2u;
    printValue(a);
    return 0;
}
