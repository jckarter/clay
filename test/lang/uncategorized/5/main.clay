import printer.(println);

define foo;

default foo(x) = 0;
override foo(x:Bool) = 1;
override foo(x:Int32) = 2;

main() {
    var v = true;
    println(foo(v) + 10*foo(true) + 100*foo(10));
    return 0;
}
