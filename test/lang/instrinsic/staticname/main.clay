
import data.vectors.generic
 as vg;
import data.hashmaps.*;
import printer.(println);
import data.strings.*;

main() {
    disp(add);
    disp(String);
    disp(Vec);
    disp(hash);
    disp(Array);
    disp(Array[Bool,10]);
    disp(HashMap);
    disp(HashMap[Int,Int]);
    disp(vg);
    disp(Record?);
    disp(disp);
}

disp(x) {
    println(x, ": ", StaticName(x));
}
