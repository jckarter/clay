// #228
// thanks to freenode:maw
import printer.(println);
import vectors.*;

variant Node (Tree, Int);
record Tree (nodes : Vector[Node]);

main() {
    var t = Tree(Vector[Node]());
    println("ok");
}
