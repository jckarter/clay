import printer.(println);
import data.vectors.*;

record Human ();

record Dog ();

variant Animal;
instance Animal (Human, Dog);

define what;
what(x:Human) = "Human";
what(x:Dog) = "Dog";

printPair(a, b) {
    println(what(a), ", ", what(b));
}


//
// extend Animal to include Cat
//

record Cat ();

instance Animal (Cat);

what(x:Cat) = "Cat";


main() {
    var v = Vector[Animal]();
    push(v, Animal(Human()));
    push(v, Animal(Dog()));
    push(v, Animal(Cat()));
 
    for (a in v)
        for (b in v)
            printPair(*a, *b);
}
