
record Node[T] = referenceType(
    (#value, T),
    (#next, Node[T]),
);

main() {
    var a = Node[Int]();
    println(null?(a));
    var b = Node[Int](10, a);
    println(null?(b));
    println(a);
    println(b);
}
