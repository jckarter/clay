import base;

overloadable foo;

overload foo(x) {
    return 0;
}

overload foo(static Int32) {
    return 1;
}

overload foo(x:Int32) {
    return 2;
}

def main() {
    var v = true;
    return foo(v) + 10*foo(Int32) + 100*foo(10);
}
