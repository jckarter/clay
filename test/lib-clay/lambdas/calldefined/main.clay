import printer.(println);

foo(p) { return x; }

main() {
    println(CallDefined?(foo));
    println(CallDefined?(foo, Int));
    println(CallDefined?(foo, Int, Bool));

    var x = 10;
    var bar = p => { return x; };
    println(CallDefined?(bar));
    println(CallDefined?(bar, Int));
    println(CallDefined?(bar, Int, Bool));
}
