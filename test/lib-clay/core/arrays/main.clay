import printer.(println);

main() {
    
    var a, b = array(3, 5, 8), array(..integers(#256));
    
    println([] == [..arrayElements(Array[UInt, 0]())]);
    println([true] == [..arrayElements(array(true))]);
    println([3, 5, 8] == [..arrayElements(a)]);

    //ArrayRef
    println(ArrayRef(a) == a);

    //Indexing
    println(b[224_uss]);
}