import printer.(println);

[T] define byval(x:T) : T;
[T] define byref(ref x:T) : ByRef[T];

override byval(x:Int) = ref x;
override byval(x:Double) = x;

override byref(ref x:Int) = ref x;
override byref(ref x:Double) = x;

main() {
    println(byval(1));
    println(byval(1.0));

    var i = 1;
    var f = 1.0;

    println(byref(i));
    println(byref(f));
}
