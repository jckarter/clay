import defaultarguments.*;
import printer.(println);

foo(..args) {
    var name, number = ..defaultArguments(["John Doe", 42], ..args);
    println(name, ": ", number);
}

main() {
    foo();
    foo("Ford Prefect");
    foo("John Romero", 666);
}
