import printer.(println);
import data.sequences.*;

main() {
    var a = integerArray(#5);
    println(a);

    var b = Array(reversed(mapped(x => 2*x, a)));
    println(b);

    var c = Array(mapped(add, zipped(a, b)));
    println(c);
}
