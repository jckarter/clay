import base.*;

enum Kind {
    GOOD,
    BAD,
    UGLY
}

main() {
    var a = UGLY;
    var b = BAD;
    var c = a > b;
    var d = a == b;
    var y = Int32(a);
    var z = Kind(y);
    println("c=", Int32(c), ", d=", Int32(d), ", y=", y);
}
