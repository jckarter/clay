import base.*;

insertionSort(a) {
    var i = 1;
    while (i < size(a)) {
        var x = at(a, i);
        var j = i;
        while (at(a,j-1) > x) {
            at(a, j) = at(a, j-1);
            j = j - 1;
            if (j == 0) break;
        }
        at(a, j) = x;
        i = i + 1;
    }
}

reverse(a) {
    var n = size(a);
    for (i in range(n / 2))
        swap(at(a, i), at(a, n-i-1));
}

main() {
    var a = Array[Int32, 1000]();
    for (i in range(size(a)))
        at(a, i) = i*2;
    for (i in range(100)) {
        insertionSort(a);
        reverse(a);
    }
    return at(a, 0);
}
