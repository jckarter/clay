
main() {
    {
        var f = File("LICENSE.txt");
        for (line in lines(f))
            print(line);
    }
    {
        var lines = Vector[String]();
        for (line in prelude.lines(File("LICENSE.txt")))
            push(lines, line);
        for (x in lines)
            print(x);
    }
}
