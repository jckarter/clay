printList(a) {
    for (i, x in enumerate(a)) {
        if (i != 0)
            print(", ");
        print(x);
    }
    println();
}


main() {
    {
        var a = range(10);
        var b = sliceUpto(a, 5);
        printList(b);
        var c = sliceFrom(a, 5);
        printList(c);

        var d = mapped(max, a, reverse(a));
        printList(d);
    }
    {
        var a = Vector(range(20));
        var b = mapped(add, a, slice(a, 1));
        printList(b);
    }
    {
        var a = Vector(range(100));
        var b = Vector(reverse(a));
        sort(b);
        println(a == b);
    }
}
