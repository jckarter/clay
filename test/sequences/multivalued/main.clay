
import deques.*;
import printer.(println);
import vectors.*;

main() {
    var a = integerArray(#5);
    var b = zipped(a, reversed(mapped(x => 2*x, a)));

    var c = Array(b);
    println(c);

    var d = Vector(b);
    println(d);

    var e = Deque(d);
    println(Vector(e));
}
