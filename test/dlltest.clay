import base.*;
import libc.(printf);

external sayHello() : Void {
    printf(cstring("Hello World\n"));
}

external factorial(n:Int):Int {
    var p = 1;
    for (i in range(2,n+1))
        p *= i;
    return p;
}
