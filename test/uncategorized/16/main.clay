
enum Kind {
    GOOD,
    BAD,
    UGLY,
}

main() {
    var a, b, = UGLY, BAD, ;
    var c = a > b;
    var d = a == b;
    var y = Int32(a);
    var z = Kind(y);
    println("c=", Int32(c), ", d=", Int32(d), ", y=", y);
}
