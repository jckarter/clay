
f(n) {
    switch (n)
    case (0) return "zero";
    case (1) return "one";
    case (2, 3)
        return "two or three";
    else
        return "other";
}

main() {
    var a = range(6);
    var b = mapped(f, a);
    var c = mapped(Tuple, a, b);
    println(Vector(c));
}
