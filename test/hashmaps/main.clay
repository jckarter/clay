
main() {
    var h = HashMap[Int,Int]();

    for (i in range(10))
        h[i*i] = i;

    for (x in range(100)) {
        if (contains?(h, x))
            println(h[x], ", ", x);
    }

    for (k,v in items(h))
        println(k, ", ", v);
}
