import base.*;
import files.mapping.*;

main() {
    static N = 500*1000;

    var file = File("tempfile.txt", CREATE);
    seek(file, N-1);
    writeByte(file, 'B');

    var mapping = FileMapping(file, 0, N);
    for (i in range(N))
        (mapping.address + i)^ = Byte((i % 26) + 65);
}
