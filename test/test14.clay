
foreach(f, a) {
    for (x in a)
        f(x);
}

sumWithLambda(a) {
    var sum = 0;
    foreach(lambda (x) { sum += x; }, a);
    println("sum with lambda = ", sum);
}

sumWithBlock(a) {
    var sum = 0;
    foreach(block(x) { sum += x; }, a);
    println("sum with block = ", sum);
}

main() {
    var a = Vector([1, 2, 3, 4, 5, 6]);
    sumWithLambda(a);
    sumWithBlock(a);
}
