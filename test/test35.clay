import paged;

main() {
    paged.initializePaging("paged.data");

    var v = Vector[paged.String]();

    for (i in range(5))
        push(v, paged.String("Hello World"));

    for (x in v)
        println(x);

    paged.printPagingState();
}
