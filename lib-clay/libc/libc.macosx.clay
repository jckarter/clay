public import libc.generated.*;

var stdin = __stdinp;
var stdout = __stdoutp;
var stderr = __stderrp;

// these are macros in libc

forceinline isnan(x:Double) : Int = Int(x != x);
forceinline isnanf(x:Float) : Int = Int(x != x);
forceinline isnanl(x:LongDouble) : Int = Int(x != x);
