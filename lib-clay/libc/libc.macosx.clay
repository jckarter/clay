public import libc.generated.*;

var stdin = __stdinp;
var stdout = __stdoutp;
var stderr = __stderrp;

// these are macros in libc

inline isnan(x:Double) : Int = Int(x != x);
inline isnanf(x:Float) : Int = Int(x != x);
inline isnanl(x:LongDouble) : Int = Int(x != x);
