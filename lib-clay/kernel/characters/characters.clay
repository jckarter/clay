import kernel.operators.*;
import kernel.numbers.*;

record Char {
    code:Int8;
}

[I|Integer?(I)]
char(c:I) = Char(to(Int8, c));

ord(c:Char) = to(Int32, c.code);

[I|Integer?(I)]
overload to(static I, c:Char) = to(I, c.code);
