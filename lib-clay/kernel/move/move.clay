import kernel.operators.*;

overloadable move;

[T]
overload move(dest:T, src:T) {
    copy(dest, src);
    destroy(src);
}

record Movable[T] {
    data:Pointer[T];
}

[T]
overload move(a:T) = Movable(&a);

[T]
overload deref(a:Movable[T]) = ref deref(a.data);
