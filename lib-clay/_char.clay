import _core;
import _numeric;

record Char(code:Int32);

char[I](c:I) if (isInteger(I)) = Char(to(Int32, c));

ord(c:Char) = c.code;

overload to[I](static I, c:Char) if (isInteger(I)) = to(I, c.code);
