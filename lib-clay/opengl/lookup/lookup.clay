import opengl.platform.*;

private _findGLFunction(name, ...names) {
    var ptr = lookupGLFunction(name);
    if (null?(ptr)) {
        return _findGLFunction(...names);
    } else {
        return ptr;
    }
}
overload _findGLFunction() { return GLCodePointer[(), ()](0); }

findGLFunction(Args, Return, ...names) {
    return GLCodePointer[Args, Return](_findGLFunction(...names));
}

