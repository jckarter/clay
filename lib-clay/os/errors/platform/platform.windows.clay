
import win32;
import win32.printer as printer;

printErrorMessageTo(stream, code:Int) {
    printer.printErrorMessageTo(stream, code);
}

errorCodeName(code:Int) {
    return win32.errorCodeName(code);
}
