public import math.native.defines.(LIBM,MathMode?,beta);
import math.native.exp.(exp);
import math.native.gamma.(lgamma);
import complex.*;

[T,U when (Float?(T,U) or Complex?(T,U)) and not MathMode?(LIBM)]
overload beta(x:T, y:U) = exp(lgamma(x)+lgamma(y)-lgamma(x+y));
