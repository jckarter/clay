//public import math.native.defines.(ldexp);
//import numbers.floats.*;
//import math.native.(scalbn);
import math.*;

[T,I | Float?(T) and Integer?(I)]
overload ldexp(value:T, i:I)
    = if(not finite?(value) or value==0.) value else scalbn(value,i);
